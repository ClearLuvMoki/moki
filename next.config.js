/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    assetPrefix: 'http://www.moonquakes.online',
    pageExtensions: ['js', 'jsx', 'ts', 'tsx'],
}

module.exports = nextConfig;
